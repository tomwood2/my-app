
// export default function timeout(ms, value) {
//     return new Promise((resolve) => setTimeout(resolve, ms));
// }

export default function timeout(ms) {
    return new Promise((resolve) => setTimeout(resolve, ms));
}
